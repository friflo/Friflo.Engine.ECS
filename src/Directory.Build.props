<Project>
  <PropertyGroup>
    <!-- Renamed default output folders obj to .obj to avoid importing them by Unity -->
    <BaseIntermediateOutputPath>.obj\$(MSBuildProjectName)\</BaseIntermediateOutputPath>
  </PropertyGroup>  
</Project>